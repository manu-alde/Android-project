<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="lightView">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="compatDialogNoTitle" parent="Theme.AppCompat.Dialog">
        <item name="windowNoTitle">true</item>
        <item name="android:textViewStyle">@style/lightView</item>
    </style>

    <style name="ButtonStyle">
        <item name="android:backgroundTint">@android:color/holo_orange_light</item>
        <item name="android:background">@drawable/bg_button</item>
    </style>

    <style name="TextButtonStyle">
        <item name="android:textColor">#DB3E0D</item>
        <item name="android:background">@drawable/ripple</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>
</resources>
